{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "yarn install --immutable",
  "buildCommand": "yarn workspace web db:generate && yarn workspace web build",
  "outputDirectory": "packages/web/.next"
}